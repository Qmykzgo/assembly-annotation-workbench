manifest {
    name = 'Qmykzgo/assembly-annotation-workbench'
    mainScript = 'main.nf'
    description = 'Evidence-aware assembly annotation and comparative genomics workbench'
    version = '0.1.0'
}

params {
    input = 'assets/test-data/annotation.csv'
    outdir = 'results/assembly-annotation'
    mode = 'real'
}

process {
    shell = ['/bin/bash', '-euo', 'pipefail']
    cpus = 1
    memory = '1 GB'
    time = '10m'
    errorStrategy = 'terminate'
}

profiles {
    test {
        params.mode = 'stub'
        params.input = 'assets/test-data/annotation.csv'
        params.outdir = 'results/test'
    }
    docker {
        docker.enabled = true
        docker.runOptions = '-u $(id -u):$(id -g)'
    }
}

trace {
    enabled = true
    file = "${params.outdir}/provenance/trace.tsv"
}

report {
    enabled = true
    file = "${params.outdir}/provenance/execution-report.html"
}
